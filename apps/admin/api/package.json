{
  "name": "happy-admin-api",
  "version": "0.1.0",
  "description": "Happy Admin API - Analytics Engine dashboard backend",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "preinstall": "node -e \"if(!process.env.INIT_CWD?.endsWith('/happy')){console.error('❌ Run yarn install from monorepo root');process.exit(1)}\"",
    "help": "echo '\nHappy Admin API - Available Scripts\n====================================\n\nDevelopment:\n  dev                Start Wrangler dev server\n\nDeployment:\n  deploy:dev         Deploy to development environment\n  deploy:prod        Deploy to production environment\n\nCode Quality:\n  typecheck          Type-check TypeScript\n  lint               Run oxlint\n  lint:fix           Run oxlint with auto-fix\n  test               Run tests with Vitest\n  test:security      Run security-specific tests\n\nDatabase:\n  db:generate        Generate Better-Auth migrations\n  db:migrate         Apply migrations to remote dev D1\n  db:migrate:prod    Apply migrations to remote prod D1\n'",
    "dev": "wrangler dev",
    "deploy:dev": "wrangler deploy --env dev",
    "deploy:prod": "wrangler deploy --env prod",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint src --config oxlint.json",
    "lint:fix": "oxlint src --config oxlint.json --fix",
    "format": "oxfmt src/",
    "format:check": "oxfmt --check src/",
    "test": "vitest run",
    "clean": "rm -rf .wrangler/state/v3/cache",
    "test:watch": "vitest",
    "test:security": "vitest run -t 'SQL Injection'",
    "db:generate": "npx @better-auth/cli generate",
    "db:migrate": "wrangler d1 migrations apply DB --env dev --remote",
    "db:migrate:prod": "wrangler d1 migrations apply DB --env prod --remote"
  },
  "keywords": [
    "cloudflare",
    "workers",
    "hono",
    "api",
    "analytics"
  ],
  "author": "Enflame Media LLC (howdy@enflamemedia.com)",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260214.0",
    "@happy/lint-rules": "workspace:*",
    "@types/node": "^25.2.3",
    "oxlint": "^1.48.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18",
    "wrangler": "^4.65.0"
  },
  "dependencies": {
    "@hono/swagger-ui": "^0.5.3",
    "@hono/zod-openapi": "^1.2.2",
    "better-auth": "^1.4.18",
    "drizzle-orm": "^1.0.0-beta.12-18d8bfb",
    "hono": "^4.11.9",
    "zod": "^4.3.6"
  }
}
