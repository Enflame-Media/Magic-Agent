# Happy Server Workers - Environment Variables
#
# This project uses Cloudflare Workers with Wrangler.
# Local development variables go in .dev.vars (gitignored).
# Production secrets are managed via `wrangler secret`.
#
# SECURITY: Never commit .dev.vars or actual secrets to version control.

# ============================================
# LOCAL DEVELOPMENT (.dev.vars)
# ============================================
# Copy these to .dev.vars for local development:

# Type: string (enum)
# Default: development
# Valid: development, staging, production
ENVIRONMENT=development

# Type: string (64-character hex)
# Default: none (required)
# Generate: openssl rand -hex 32
# Note: Use a different secret for each environment
HAPPY_MASTER_SECRET=your-local-development-secret

# Type: string (64-character hex)
# Default: none (deprecated)
# Note: DEPRECATED - Use HAPPY_MASTER_SECRET instead
# Still supported for backward compatibility but will be removed in future
# HANDY_MASTER_SECRET=

# Type: string (API key)
# Default: none (optional feature)
# Get from: https://elevenlabs.io/
# Note: Required for voice synthesis features
# ELEVENLABS_API_KEY=your-elevenlabs-key

# ============================================
# GitHub Integration (Optional)
# ============================================

# Type: integer
# Default: none (optional feature)
# Get from: GitHub App settings
# GITHUB_APP_ID=

# Type: string (PEM format)
# Default: none (optional feature)
# Note: Multi-line - use base64 encoding or escape newlines
# GITHUB_PRIVATE_KEY=

# Type: string
# Default: none (optional feature)
# Get from: GitHub OAuth App settings
# GITHUB_CLIENT_ID=

# Type: string
# Default: none (optional feature)
# Get from: GitHub OAuth App settings
# GITHUB_CLIENT_SECRET=

# Type: string (40-character hex recommended)
# Default: none (optional feature)
# Generate: openssl rand -hex 20
# GITHUB_WEBHOOK_SECRET=

# ============================================
# PRODUCTION SECRETS (via wrangler secret)
# ============================================
# These are set using `wrangler secret put` commands:
#
# Required for production:
#   Type: string (64-character hex)
#   wrangler secret put HAPPY_MASTER_SECRET --env prod
#
# Optional for production:
#   Type: string (API key)
#   wrangler secret put ELEVENLABS_API_KEY --env prod
#
#   Type: string (PEM format)
#   wrangler secret put GITHUB_PRIVATE_KEY --env prod
#
#   Type: string
#   wrangler secret put GITHUB_CLIENT_SECRET --env prod
#
#   Type: string (40-character hex)
#   wrangler secret put GITHUB_WEBHOOK_SECRET --env prod
#
# Management commands:
#   List all secrets:   wrangler secret list --env prod
#   Delete a secret:    wrangler secret delete SECRET_NAME --env prod

# ============================================
# CLOUDFLARE BINDINGS (configured in wrangler.toml)
# ============================================
# These are configured in wrangler.toml, not as environment variables:
#
# Type: D1 Database binding
# Name: DB
# Note: SQLite-compatible database
#
# Type: R2 Bucket binding
# Name: UPLOADS
# Note: S3-compatible object storage
#
# Type: Durable Object binding
# Name: CONNECTION_MANAGER
# Note: Stateful WebSocket connection management
#
# See wrangler.toml for environment-specific configurations.
