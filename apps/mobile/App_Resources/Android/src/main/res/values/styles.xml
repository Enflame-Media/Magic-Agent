<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Material Components Theme for Happy App -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/ns_primary</item>
        <item name="colorPrimaryVariant">@color/ns_primaryDark</item>
        <item name="colorOnPrimary">@color/ns_white</item>

        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/ns_accent</item>
        <item name="colorSecondaryVariant">@color/ns_accent</item>
        <item name="colorOnSecondary">@color/ns_white</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">@color/ns_primary</item>

        <!-- Default background -->
        <item name="android:windowBackground">@color/ns_background</item>
    </style>

    <!-- Launch screen theme -->
    <style name="LaunchScreenTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_screen</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <!-- Dark mode overrides -->
    <style name="AppTheme.Dark" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/ns_primary</item>
        <item name="colorPrimaryVariant">@color/ns_primaryDark</item>
        <item name="colorSecondary">@color/ns_accent</item>
        <item name="android:windowBackground">@color/ns_backgroundDark</item>
    </style>
</resources>
