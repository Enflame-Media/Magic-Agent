/**
 * Pinia Store Configuration
 *
 * Root store module that exports all application stores.
 * Pinia is Vue 3's recommended state management solution.
 */

// Re-export stores for convenient imports
// Example: import { useSessionStore } from '@/stores';

// Placeholder export until stores are implemented
export {};
